-- Crypt-MM2-Legit | Config — paste ALL your decal IDs here

_G.Config = {
    -- ── Background (the UI surface itself) ──────────────────────────────────
    -- Upload image at roblox.com/develop > Decals > Upload
    -- Copy the NUMBER from the URL after upload
    BackgroundDecal     = "rbxassetid://10341849885",

    -- ── Minimized icon (shown when UI is hidden) ─────────────────────────────
    MinimizeIconDecal   = "rbxassetid://2533010550",

    -- ── Sidebar tab icons (replace 0 with your decal IDs) ────────────────────
    Icon_Main           = "rbxassetid://127186542721681",
    Icon_Aimbot         = "rbxassetid://73476586405100",
    Icon_ESP            = "rbxassetid://16149070679",
    Icon_LocalPlayer    = "rbxassetid://124871982298256",
    Icon_Visuals        = "rbxassetid://18979524669",

    -- ── Keys ─────────────────────────────────────────────────────────────────
    ToggleKey           = Enum.KeyCode.RightShift,

    -- ── Window ───────────────────────────────────────────────────────────────
    WindowSize          = UDim2.new(0, 540, 0, 420),
    IconSize            = UDim2.new(0, 54, 0, 54),
    StartPosition       = UDim2.new(0.5, -270, 0.5, -210),
}
